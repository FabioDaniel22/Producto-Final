// Tipos de for (Bucle)
/**estructura
 * for(inicializacion; condicion; actualizacion){
 *          Bucle
 * }
 * 
 * i = i + i;
 * i += i;
 * i++
 * i--
 */

//for
for(let i = 0; i < 10; i++){
//esto es un bucle
console.log[1];
}

let lista = [1, 12, 34, 56, 500, 454]
for(let i = 0; i < lista.length /*longitud de la cadena*/; i++){
      // esto es un bucle
      console.log(lista[1]);
}

//forEach
lista.forEach( valor => /*Arrow function*/{
    console.log(valor);
})

//for..of
for (let valor of lista){
     console.log(valor);
}

//for..in
let persona = 